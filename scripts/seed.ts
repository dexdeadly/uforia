/**
 * Prisma seed entry point (run via scripts/safe-seed.ts).
 *
 * Uforia's data comes from document ingestion rather than fixture rows —
 * there is nothing to seed. Populate the database with:
 *
 *   npm run ingest                      # official government UAP releases
 *   npm run ingest -- --dir <folder>    # local PDFs/images
 */

console.log('Nothing to seed. Populate the database with `npm run ingest` instead.')
